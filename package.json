{
  "name": "ai-spending-analyzer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "python -m streamlit run app.py --server.port 5000 --server.address 0.0.0.0",
    "start": "python -m streamlit run app.py --server.port 5000 --server.address 0.0.0.0"
  }
}
